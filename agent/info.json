{
  "author": [
    "Eris"
  ],
  "description": "LLM Agent designed to interact with all of the bot's functionality in an intelligent way",
  "name": "Chat",
  "short": "Use OpenAI's ChatGPT to respond to the conversation",
  "requirements": [
    "openai==1.76.2",
    "langchain==0.3.27",
    "langgraph==0.5.4"
  ],
  "install_msg": "Ensure that you set the openAI token, using `[p]set api`, and then the name of the secret should be `openai`, while the value needs to be in the format `key <token>`. The same token is shared between both this cog and the `chat` cog",
  "ready": false
}
